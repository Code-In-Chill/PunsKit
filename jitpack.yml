jdk:
  - openjdk21
install:
  - ./gradlew :core-kit:shadowJar -Pversion=${VERSION:-${JITPACK_VERSION:-develop-SNAPSHOT}}
  - ./gradlew :core-kit:publishToMavenLocal -Pversion=${VERSION:-${JITPACK_VERSION:-develop-SNAPSHOT}}
